package julie.calendar.Feature;

import android.view.View;

/**
 * интерфейс для задания поведения поля при клике на него (в режиме редактирования)
 */
public interface FeatureOnClickEdit {
    void onClickEdit(View view);
}